# Archived root markdown files

These markdown files were previously stored at the repository root.
They were moved here during repository-hygiene cleanup to reduce root clutter and make the actively maintained docs/plans easier to find.

This folder is archival by default: keep provenance, but do not treat every file here as living documentation.
