# Data Pipeline Dependencies for Finance Feedback Engine 2.0
#
# Install with: pip install -r requirements-pipeline.txt
#
# These are additional dependencies for the production-grade data pipeline:
# - Batch/streaming ingestion
# - Delta Lake storage (ACID transactions)
# - dbt transformations
# - Great Expectations data quality
# - Airflow orchestration
# - Prometheus monitoring

# ============================================================================
# Delta Lake Storage (ACID transactions, time travel, partitioning)
# ============================================================================
delta-spark==3.0.0  # Delta Lake connector for Spark
pyspark==3.5.0      # Apache Spark for distributed processing
pyarrow>=15.0.0     # Fast columnar storage (Parquet)

# ============================================================================
# Data Quality Framework (Great Expectations)
# ============================================================================
great-expectations>=0.18.0  # Data validation and profiling

# ============================================================================
# Data Transformation (dbt - Data Build Tool)
# ============================================================================
dbt-core>=1.7.0     # Transformation framework
dbt-spark>=1.7.0    # Spark adapter for dbt

# ============================================================================
# Orchestration (Apache Airflow)
# ============================================================================
apache-airflow>=2.7.0           # Workflow orchestration
apache-airflow-providers-amazon>=8.0.0  # AWS providers (S3, EMR, MSK)
apache-airflow-providers-apache-spark>=4.0.0  # Spark integration
apache-airflow-providers-dbt-cloud>=3.0.0     # dbt Cloud integration

# ============================================================================
# Streaming Ingestion (Kafka)
# ============================================================================
kafka-python>=2.0.2          # Kafka producer/consumer
confluent-kafka>=2.3.0       # High-performance Kafka client

# ============================================================================
# Monitoring & Observability
# ============================================================================
prometheus-client>=0.19.0    # Prometheus metrics exporter
grafana-client>=4.0.0        # Grafana API client (dashboard provisioning)

# ============================================================================
# Cloud Storage & Infrastructure
# ============================================================================
boto3>=1.34.0      # AWS SDK (S3, EMR, MSK, Athena)
s3fs>=2024.2.0     # S3 filesystem interface for Pandas/Spark
minio>=7.2.0       # MinIO client (local S3-compatible storage)

# ============================================================================
# Utilities
# ============================================================================
sqlalchemy>=2.0.0           # Database abstraction (Airflow metadata)
psycopg2-binary>=2.9.0      # PostgreSQL driver (Airflow backend)
cryptography>=42.0.0        # Encryption utilities
