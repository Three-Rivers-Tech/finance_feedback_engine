apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "ffe-backend.serviceAccountName" . }}
  labels:
    {{- include "ffe-backend.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: {{ include "ffe-backend.fullname" . }}
  labels:
    {{- include "ffe-backend.labels" . | nindent 4 }}
rules:
  # Allow reading config maps and secrets
  - apiGroups: [""]
    resources: ["configmaps", "secrets"]
    verbs: ["get", "list", "watch"]
  # Allow reading service endpoints
  - apiGroups: [""]
    resources: ["endpoints"]
    verbs: ["get", "list"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ include "ffe-backend.fullname" . }}
  labels:
    {{- include "ffe-backend.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: {{ include "ffe-backend.fullname" . }}
subjects:
  - kind: ServiceAccount
    name: {{ include "ffe-backend.serviceAccountName" . }}
    namespace: {{ .Release.Namespace }}
