# Legacy root diagnostic scripts

These files were previously stored at the repository root as `test_*.py`, but they behave more like ad hoc diagnostic/demo scripts than first-class pytest suites.

They were relocated here during repository-hygiene cleanup to reduce root clutter and avoid implying that they are part of the primary `tests/` tree.
