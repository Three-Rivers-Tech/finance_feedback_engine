global:
  scrape_interval: 30s  # Scrape metrics every 30 seconds
  evaluation_interval: 30s

scrape_configs:
  - job_name: 'finance-feedback-engine'
    static_configs:
      - targets: ['host.docker.internal:8000']  # FastAPI server
    metrics_path: '/metrics'
