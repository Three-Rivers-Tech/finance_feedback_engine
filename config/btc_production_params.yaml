# BTC/USD M5 Production Parameters
# Optimized via Optuna - 100 trials, 30 days historical data
# Results: 84.44% WR, 2.27 PF, +0.92% return
# Gemini Rating: 9/10 - Production Ready

symbol: BTC_USD
timeframe: M5  # 5-minute candles
environment: development  # $50 max per trade

# Optimized Parameters (from THR-225)
stop_loss_pct: 0.05      # 5.0% stop loss
take_profit_pct: 0.012   # 1.2% take profit
position_size_pct: 0.028  # 2.8% of account balance

# Risk Controls
max_position_usd: 50.0   # Hard cap in dev mode
risk_per_trade_pct: 0.02  # 2% account risk max
max_daily_trades: 10
max_concurrent_positions: 1

# Performance Targets
target_win_rate: 0.70     # 70% minimum (proven: 84.44%)
target_profit_factor: 1.50  # 1.5 minimum (proven: 2.27)
max_drawdown_pct: 0.10    # 10% max drawdown before pause

# Monitoring
alert_on_loss: true
alert_threshold_usd: 40.0  # Alert if single loss >$40
daily_pnl_report: true

# Validation From Optimization
validation:
  trials: 100
  best_trial: 66
  win_rate: 0.8444
  profit_factor: 2.27
  total_trades: 45
  return_pct: 0.0092
  sharpe_ratio: 0.0
  max_drawdown: 0.0
  parameter_importance:
    stop_loss: 0.546
    take_profit: 0.442
    position_size: 0.012
  gemini_rating: 9
  approved_date: "2026-02-13"
